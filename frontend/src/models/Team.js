class Team {
  constructor({
    id,
    name,
    country,
    competition,
    logo,
  }) {
    this.id = id;
    this.name = name;
    this.country = country;
    this.competition = competition;
    this.logo = logo;
  }
}

export default Team;